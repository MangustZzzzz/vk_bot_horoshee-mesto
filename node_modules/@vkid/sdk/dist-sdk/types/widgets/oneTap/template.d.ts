import { WidgetParams } from "../../core/widget";
import { OneTapParams, OneTapStyles } from './types';
type OneTapTemplateParams = Required<OneTapStyles> & Pick<OneTapParams, 'skin'> & Pick<WidgetParams, 'scheme' | 'lang'> & {
    openFullAuth?: void;
    iframeHeight?: number;
};
export declare const getOneTapTemplate: ({ width, height, iframeHeight, borderRadius, openFullAuth, skin, scheme, lang, }: OneTapTemplateParams) => (id: string) => string;
export {};
